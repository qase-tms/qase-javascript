---
phase: 10-core-spec-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - qase-javascript-commons/src/models/stats.ts
  - qase-javascript-commons/src/reporters/report-reporter.ts
  - qase-javascript-commons/src/writer/fs-writer.ts
  - qase-javascript-commons/test/reporters/report-reporter.test.ts
  - qase-javascript-commons/test/writer/fs-writer.test.ts
autonomous: true

must_haves:
  truths:
    - "Report writer produces run.json (or run.jsonp) file instead of report.json"
    - "Report title field is 'Test run' matching spec and Python reference"
    - "Stats model includes blocked and invalid fields, does not include broken"
    - "TestStatusEnum.invalid maps to stats.invalid counter"
    - "TestStatusEnum.blocked maps to stats.blocked counter"
    - "Stats still includes muted field per spec"
  artifacts:
    - path: "qase-javascript-commons/src/models/stats.ts"
      provides: "Stats interface with blocked, invalid, muted fields"
      contains: "blocked.*number"
    - path: "qase-javascript-commons/src/reporters/report-reporter.ts"
      provides: "Report generation with correct title and status mapping"
      contains: "Test run"
    - path: "qase-javascript-commons/src/writer/fs-writer.ts"
      provides: "File writer producing run.json"
      contains: "run\\."
  key_links:
    - from: "qase-javascript-commons/src/reporters/report-reporter.ts"
      to: "qase-javascript-commons/src/models/stats.ts"
      via: "Stats interface usage in complete()"
      pattern: "stats\\.invalid|stats\\.blocked"
    - from: "qase-javascript-commons/src/reporters/report-reporter.ts"
      to: "qase-javascript-commons/src/writer/fs-writer.ts"
      via: "writeReport() call"
      pattern: "this\\.writer\\.writeReport"
---

<objective>
Fix report file naming, report title, and stats model to match the Qase Report specification.

Purpose: Align the root report file output (run.json), its title field, and the stats counters with the cross-language spec (ROOT-01, ROOT-02, STAT-01 through STAT-05).
Output: Updated Stats interface, corrected status mapping in report-reporter, and run.json file naming in fs-writer, with passing tests.
</objective>

<execution_context>
@/Users/gda/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Spec references (read these first to confirm exact field names):
@../specs/report/schemas/root.yaml

Python reference for title and stats:
@../qase-python/qase-python-commons/src/qase/commons/reporters/report.py (line 168: title="Test run")
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Stats interface and report-reporter status mapping</name>
  <files>
    qase-javascript-commons/src/models/stats.ts
    qase-javascript-commons/src/reporters/report-reporter.ts
  </files>
  <action>
1. Update `Stats` interface in `stats.ts`:
   - Remove `broken` field
   - Add `blocked` field (type: number)
   - Add `invalid` field (type: number)
   - Keep `muted` field (it IS in the spec)
   - Final fields: `passed`, `failed`, `skipped`, `blocked`, `invalid`, `muted`, `total`

2. Update `report-reporter.ts` `complete()` method:
   - Change `title: 'Test report'` to `title: 'Test run'` (ROOT-02, matches Python reference line 168)
   - Update stats initialization to use new field names: `blocked: 0, invalid: 0` instead of `broken: 0`
   - Fix status mapping in the switch statement:
     - `TestStatusEnum.invalid` -> `report.stats.invalid++` (was `report.stats.broken++`) (STAT-04)
     - `TestStatusEnum.blocked` -> `report.stats.blocked++` (was `report.stats.muted++`) (STAT-05)
   - Keep `TestStatusEnum.blocked` separate from muted. The `muted` field stays but is NOT incremented by blocked status (it may be used differently or stay 0).

Note: The spec has `blocked`, `invalid`, AND `muted` as separate stats fields. Currently the code incorrectly maps `invalid` -> `broken` and `blocked` -> `muted`. After fix: `invalid` -> `invalid`, `blocked` -> `blocked`, `muted` stays at 0 (no status currently maps to it, which is correct per spec - muted is a separate concept from blocked).
  </action>
  <verify>
Run `cd /Users/gda/Documents/github/qase-tms/qase-javascript/qase-javascript-commons && npm run build` to verify no TypeScript errors.
  </verify>
  <done>
Stats interface has `blocked`, `invalid`, `muted` fields (no `broken`). Report title is "Test run". Status mapping: invalid->invalid, blocked->blocked.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix report file name and update all tests</name>
  <files>
    qase-javascript-commons/src/writer/fs-writer.ts
    qase-javascript-commons/test/reporters/report-reporter.test.ts
    qase-javascript-commons/test/writer/fs-writer.test.ts
  </files>
  <action>
1. Update `fs-writer.ts` `writeReport()` method:
   - Change line 95: `const filePath = path.join(this.path, 'report.${this.format}')`
   - To: `const filePath = path.join(this.path, 'run.${this.format}')` (ROOT-01)
   - This produces `run.json` or `run.jsonp` depending on format setting.

2. Update `report-reporter.test.ts`:
   - Update mock writer return value from `'/mock/report.json'` to `'/mock/run.json'`
   - Update the stats assertion in "should correctly calculate stats" test:
     - Change `{ total: 5, passed: 1, failed: 1, skipped: 1, broken: 1, muted: 1 }`
     - To: `{ total: 5, passed: 1, failed: 1, skipped: 1, invalid: 1, blocked: 1, muted: 0 }`
     - (invalid maps to invalid counter, blocked maps to blocked counter, muted stays 0)
   - Update the "should write report and log path" test to check for `run.json` in log output
   - Update the title assertion if any test checks for 'Test report' -> 'Test run'

3. Update `fs-writer.test.ts`:
   - In `writeReport` describe block, update all `Stats` objects:
     - Change `{ total: 0, passed: 0, failed: 0, skipped: 0, broken: 0, muted: 0 }`
     - To: `{ total: 0, passed: 0, failed: 0, skipped: 0, blocked: 0, invalid: 0, muted: 0 }`
   - Change expected file path assertions from `'test-path/report.json'` to `'test-path/run.json'`
   - Update `mockWriteFileSync` assertion from `'test-path/report.json'` to `'test-path/run.json'`
  </action>
  <verify>
Run `cd /Users/gda/Documents/github/qase-tms/qase-javascript/qase-javascript-commons && npm test` — all tests must pass.
  </verify>
  <done>
FsWriter produces `run.json`/`run.jsonp` files. All tests pass with updated stats fields and file names.
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/gda/Documents/github/qase-tms/qase-javascript/qase-javascript-commons && npm run build` passes with no errors
2. `cd /Users/gda/Documents/github/qase-tms/qase-javascript/qase-javascript-commons && npm test` passes all tests
3. Grep confirms: `grep -r "broken" qase-javascript-commons/src/models/stats.ts` returns nothing
4. Grep confirms: `grep "run\\." qase-javascript-commons/src/writer/fs-writer.ts` shows `run.${this.format}`
5. Grep confirms: `grep "Test run" qase-javascript-commons/src/reporters/report-reporter.ts` shows title
</verification>

<success_criteria>
- Stats interface has fields: passed, failed, skipped, blocked, invalid, muted, total (no broken)
- report-reporter.ts complete() sets title to "Test run"
- report-reporter.ts maps invalid->stats.invalid, blocked->stats.blocked
- fs-writer.ts produces run.json (not report.json)
- All existing tests pass with updated assertions
- Build succeeds with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-core-spec-alignment/10-01-SUMMARY.md`
</output>
