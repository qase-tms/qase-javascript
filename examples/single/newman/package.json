{
  "name": "examples-newman",
  "private": true,
  "scripts": {
    "test": "QASE_MODE=${QASE_MODE:-off} newman run ./api-collection.json -r qase",
    "test:data": "QASE_MODE=${QASE_MODE:-off} newman run ./api-collection.json -r qase -d data.json"
  },
  "devDependencies": {
    "newman": "^6.2.2",
    "newman-reporter-qase": "^2.2.0"
  }
}
